# Apple 界面与会话稳定性实施记录

> 状态更新（2026-09-10）：用户已选择恢复原有液体玻璃 UI，并保留 bug 与性能修复。当前收尾、验证与安装结果以 [UI 回退实施记录](2026-09-10-ui-rollback.md) 为准；下文保留原方案的过程记录。

基线：`5c719f9`，开始时工作区干净。实现用户批准的合并计划；不提交、不推送，仅安装与验证 `127.0.0.1:5557`。不操作真实选退课或抢课。

## 固定约束

- 保留五个主入口、现有 Q 弹参数、回弹、选择器分裂与吸收、静止终态。
- 基础修复完成后再修改视觉。现有业务协议、抢课调度和配额规则保留。
- 用户自选壁纸保留。默认内容方向为 Apple 系统内容层。
- 登录失效首次轻弹窗，随后保留重新登录入口；成功恢复后旧提示全部失效。

## 进度

- [x] 基线构建、5557 实际页面与真实时钟性能采样（三轮）。
- [x] 会话版本、共享续期、旧结果失效及单元回归（设备流程待最终回归）。
- [ ] 过期弹层及重新登录返回原页。
- [ ] 排版、材质来源、六类页面重排。
- [ ] 状态矢量、连续转场、控件与辅助功能回归。
- [ ] Debug / Release / AndroidTest / lint 检查。
- [ ] 5557 三轮性能复测、画面和交互验收。

## 证据规则

### 当前暂停点

用户要求「先不要测试了」，已停止 5557 上的设备测试，不再启动后续测试。工作区保留全部实现，不提交、不推送。

后续用户不认可当前标题、对齐与不透明卡片，要求先看新设计图。现进入第二轮设计提案；当前 UI 不作为已获认可的最终方案。新的主题对照与必须合并的未完成工作见 [第二轮 UI 设计提案与合并待办](2026-09-08-ui-round-2.md)，测试继续暂停。

- 已实现会话版本过滤、自动/手动共享续期、巡检取消与去重、页面加载编号、过期提示及登录 Activity 返回流程。
- 已实现中性默认背景、稳定内容表面、共用标题间距、课程/已选/课表/抢课/成绩/设置及登录页的布局调整。
- 已实现密码、刷新、执行、队列结果矢量动效，菜单展开回收，以及模态统一进度与当前页面采样。
- `ui-validation-build.log`：该检查点的 Debug、Release、AndroidTest、lint、UiPreview 和单元测试通过；之后仍有布局与测试驱动调整，不能把该结果当作最终源码的完整构建验收。
- 会话专项单元测试共 25 项通过：续期 9、提示状态 5、请求编号 3、适配器替换 2、网络回调 2、巡检调度 4。
- `liquid-device.log`：12 项设备交互测试通过，包括图标反转、队列成功中间帧、导航连续切换、取消及弹层返回。之后模态样式仍有调整。
- `session-pages-device-3.log`：6 项中 4 项通过；Cookie 恢复、旧广播失效、登录取消/成功返回已有通过样本，另有定位与选中状态断言失败。
- `ui-flows-matrix-device.log`：修正测试驱动后，选择器延后提示与 Cookie 恢复通过，登录页面文字定位再次失败；运行至页面状态测试时按用户要求强制停止。日志末尾的 `Process crashed` 是这次主动停止导致，不作为应用自然崩溃证据。
- 适配矩阵尚未执行，最终优化构建三轮性能复测尚未执行；设备上未发现 TalkBack 包，实际读屏验证也未完成。
- 停止时字体缩放为 `1.0`，动画缩放为 `1`。适配测试尚未开始，未改变显示尺寸或用户壁纸。

当前不是完整验收完成状态。后续需先处理剩余设备流程失败，再继续适配、最终构建和性能对照；本次暂停不补跑测试。

### 会话中间检查点

- `session-checkpoint-build.log`：单元测试、Debug、Release、Debug AndroidTest、lint 构建通过。
- 共享续期 9 项、过期提示状态 5 项、请求编号 3 项、MockWebServer 适配器替换 2 项测试通过。
- 中间源码保存于 `artifacts/apple-refresh-20260908/session-checkpoint/app`；此时公共视觉材质与动效参数未改。
- 优化基线补入工作区现有但被 Git 忽略的 ProGuard 配置；增加测试运行库保留规则，前后使用相同配置。
- 5557 使用 MuMu 多显示器。默认 ADB 截图是桌面，已改按 Activity 的实际显示器注入手势并使用 PixelCopy 截取窗口。
- 真实时钟测试移除 ActivityScenario 的空闲等待，避免在测量空闲问题之前就被阻塞。最初失败或未产出数据的采样不计入验收。
- `baseline-real-clock.log`：1 项真实时钟设备测试通过，118 秒。五页截图与 12 份原始样本位于 `baseline-measurements`。
- 基线三轮 P95 中位数：导航 196.97ms、成绩滑动 229.11ms、课表滑动 275.79ms、静止 126.49ms；静止业务重组均为 0，但窗口持续出帧，需继续检查渲染路径。此处为 API 35 MuMu 的本次原始测量。

性能测试使用 UiAutomator、真实显示时钟与优化构建，包含导航、成绩滚动、课表滚动、静止十秒；不使用手动推进 Compose 时钟的数据作为帧率基线。截图前确认包名与页面。仅引用本轮结果；未验证项保持未完成。
