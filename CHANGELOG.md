# 更新日志 (Changelog)

> 1.0.36 – 1.0.66 期间本文件未随版本更新（曾被 `.gitignore` 屏蔽），这一段的变更请查阅 Git 提交历史。
> 自 1.0.68 起，每个版本的更新日志以 `release-notes/vX.Y.Z.md` 为唯一数据源，由 CI 扇出到本文件、GitHub Release 与应用内更新提示。
> 1.0.67 未发布：该 tag 的流水线在版本号校验步骤失败，未产出任何 Release，内容顺延至 1.0.68。

## [1.2.0] - 2026-09-24

本版本两件事：课表页整体对齐上游 v1.0.87 的形态（日期入口、日/周各自翻页、顶栏收成菜单、日视图 agenda 化），以及「周课表」组件下线换成「今日时间轴」。小组件的渲染实现没有采用上游那套，仍然是本仓库自己的三个 Glance 组件。

### 头部：日期成为一级入口

`WeekHeaderCompact` 的标题列从「第 N 周 + 学期分段控件」换成「日期 + 周次状态」两行，学期切换移入弹窗。

- 点标题弹出 `ScheduleCalendarPanel`（日期与学期）：`本学期 / 下学期` 分段、`< 选择周次 >` 前后翻、1–25 周网格直接跳。
- 副标题按状态给话：`开学日期待设置` / `尚未开学` / `下学期 · 第 N 周` / `本学期已结束`。它与既有一次性弹窗、`LocalSemesterAnchorNotice` 内联条并存，缺锚点这件事仍然不许静默。
- 右侧原来 5 枚玻璃芯片（日/周、上一周、下一周、导出、设置）换成 `日 | 周` 分段控件 + `SystemActionMenu`。翻周改由横向滑动与弹窗承担。
- 收起态（`collapseFraction >= 0.5`）分段控件整块淡出，其入口出现在菜单最前两条（切换视图、回到今天）。
- 日期条从只读变成可点：`ScheduleDateStrip` 每列 `weight(1f)`，与网格共用 `scheduleGridPadding()` / `scheduleTimeColumnWidth()` 这一对函数——横向几何只有一处定义，星期与课程列才继续对得齐。选中与今天都用主色，今天额外带圆点。
- 翻到别周时时间列那一格出现「今天」按钮。

### 统一 pager 与日视图 agenda 化

`ScheduleScreen` 的周视图 pager 与独立的日视图合成一个 pager，页数随模式变化：周视图 25 页（一页一周），日视图 175 页（一页一天）。

- 页码换算 `pageOf / weekOf / dayOf` 一律以 `renderedDayView` 为准。`ScheduleWeekPagerSync` 保持只认「第几周」，页码换算留在视图侧，未改动这个有测试的类。
- 换视图分两拍：淡出 → 翻转渲染模式并 `withFrameNanos` 让出一帧 → 落页码 → 淡入。**让帧这步不能省**：`scrollToPage` 读的是上一次布局算出的页数，不让帧就会被旧的 25 页钳住，日视图里第 40 页会落到最后一页。
- 「正在上课」「下一节」「还剩几堂」统一由 `ScheduleAgenda` 算（绝对毫秒，不是节次序号），日视图与网格共用同一份，不会出现摘要说还剩两堂、网格里那节已画成下课。节次时间取页面正在显示的 `periodTimes`，与网格同源。
- 日视图新增 `ScheduleDayList`：日标题、`今日 N 堂 · 还剩 M 堂 / · 已结束 / · N 堂周次待核对`（非今天则「当日」）、无课与越界的分叉话术、下一节课的标记。
- `showWeekend` 只影响周视图列数（`dayCount` 贯穿 `ScheduleGrid` / `TimetableLayout` / `TimetableBackground` / 日期条）。日视图一天一页，日期条必须照旧摆满 7 格，否则翻到周六周日时选中项落在条外。

### 显示偏好按账号存

新增 `ScheduleDisplayStore`（`schedule_display`）：`dayView` 与 `showWeekend` 按 `currentAccountStorageKey` 各存一份，账号为空时拒写——本轮之前有过一次「登出把本地课表变成读不到的孤儿数据」，归属键与登录态必须分开这条不再重犯。**默认值是周视图**，与上游 `dayView = true` 刻意不同。

### 小组件：周课表下线，换成今日时间轴

整周网格在桌面尺寸下可读性差（七列八节、字号 9sp），换成从今天起往后几天的时间轴：左侧上课时刻、一枚课程色点、右侧课名与教室；每天标出日子（跨周时写真实星期几而不是「明天」），正在上的一节高亮，整列 `LazyColumn` 可滚动。今天已下课的课退出队列。

- 新增 `WidgetToday.timeline()`，`cellsForWeek` / `WidgetCell` 与 `weekGridRows` 那套「按高度反推行数」的推导一并删除（可滚动列表不再按高度截断）。`courseEndMillis` 泛化成 `timeOnDay(daySample, zone, time)`，起止两端与跨天共用。
- **接收者类名 `WeekWidgetReceiver` 原样保留**，只换内容与 label / provider 资源。系统记录的是组件名，改名会让桌面上已有的实例升级后变成「不可用」的灰块。这条约束写进了 `TimelineWidget.kt` 与清单注释。
- 补上「下课闹钟」（`WidgetRefreshScheduler` + `nextRefreshAt`）：整列都按「现在几点」排，系统定时刷新最短 30 分钟，不自定闹钟高亮会一直停在上一节。
- 新增应用内入口 `ScheduleWidgetPicker`：列出本仓库三个组件，文案取自 `widget` 模块的 label / description，尺寸写真实的 4×2 / 4×2 / 5×3；`requestPinAppWidget` 不可用时用 `GlassToaster` 说明长按桌面添加。预览走静态图（Glance 无法像 RemoteViews 那样在应用内实时摆出来），「今日时间轴」暂无实拍图，该卡片只有文字，没有伪造示意图。

### 真机上验出来的两处

- 标题列上的 `clip(RoundedCornerShape(14.dp))` 会把贴着左下角的「第 N 周」切掉一个四分之一圆（14dp 在 3.25 密度约 45px）。已移除并加注释禁止再加回来——它只影响点按高亮的形状。
- 日期标题的测量沿用 `bodyLarge.copy(fontSize = 26.sp)`，继承了主题 24sp 行高，而渲染写的是 31sp：量小画大，`Column` 把剩余高度往下传，被挤掉顶部的正是副标题。系统字体 1.0 时被 60dp 行高下限掩盖，1.3 时削掉约 4.5dp。抽出 `headerDateStyle(collapse)` 让测量与渲染同源，与 `weekdayStyle` 同一套路。星期行余量按真实构成（圆点 + 两处间距 + 选中底衬）从 12dp 提到 14dp。

### 验证

- `:widget:test` 62 条通过，其中新增 `WidgetTimelineTest` 9 条：下课退出队列、正在上高亮、跨天顺序与日头、单双周随周次奇偶切换（从被删的网格测试里接过来）、缺节次时间的课整条丢弃、窗口内无课为空、校区前缀裁剪、同刻多门课顺序只由数据决定。
- `:app:testDebugUnitTest` 与 `:app:assembleRelease` 通过。设备测试（`ScheduleAdaptationDeviceTest` / `ScheduleMotionDeviceTest` / `FeedbackFixesDeviceTest`）已按新签名与控件改好断言，但未在真机跑过。
- **真机确认**（vivo / OriginOS，release 签名包覆盖 v1.1.8 安装）：日期入口与选周跳转、日期条点选、日视图一天一页与翻页落点、收起态菜单两条、日/周分段、`…` 桌面组件面板与添加、显示周末五列、深浅两主题与大字体、离线查看模式，均正常；「第」字缺角在移除 clip 后消失。
- 未确认：桌面上旧「周课表」实例升级后是否原地变成时间轴（保留接收者类名这条路径的前提），以及 `requestPinAppWidget` 在该桌面的实际表现。

## [1.1.8] - 2026-09-20

本版本三处改动：教务登录的跨协议跳转、周视图星期条的日期行、桌面小组件改为可滚动。

### 教务登录：同域 http 跳转升级回 https

部分学校的教务挂在反向代理后面，生成绝对 `Location` 时一律写 `http://`（实测九江学院 `zhjw1.jju.edu.cn` 的每个 302 都是如此，加 `X-Forwarded-Proto: https` 也不改）。学校配置的协议是 `https` 时，`AcademicUrlPolicy.isAllowed` 会在协议判定处直接拒绝这一跳，而 `AcademicHttpTransport.ensureAllowed` 先判白名单、后判降级，于是降级被报成「跳到了未配置的域名」——那句专门写的降级提示是不可达的死代码。

- `AcademicUrlPolicy` 新增 `HostMatch` 与 `hostMatch()`，只看域名与端口、忽略协议；`isAllowed()` 改为委托它，判定口径逐条保持原样（含「条目未写端口时按请求协议补默认端口」这一既有行为），所以 WebView 的 Cookie 导出边界不变。
- 新增 `httpsUpgrade()`：同域名、默认端口的 http 跳升级成 https 再跟随。带显式端口的地址不做猜测，避免把 `http://host:8080` 猜成没人监听的 `https://host:8080`。
- `AcademicHttpTransport` 的重定向分支接入升级，且**一条链只升级一次**，防止与坚持写 http 的服务器互相 302。
- `ensureAllowed` 按「未配置域名 / 未配置端口 / HTTP 明文降级」分别报错并带上目标 host；`SSLHandshakeException` 单独成错（`UNTRUSTED_URL`）并跳过无意义的读重试。
- `AcademicStatus.UNTRUSTED_URL` 的中文映射改宽，覆盖上述四种分叉。

未改动：`school.allowedAcademicHosts` 在 transport 构造时快照的问题（登录中途补加的域名本轮仍不生效），以及把 `UNTRUSTED_URL` 改走 WebView 引导的控制流。前者对本案学校无影响（它不走 CAS），后者会连带把 `detect()` 中途改过的配置落盘固化。

### 周视图：日期行从未显示

`CompactWeekdayLabel` 的两个 `Text` 只传 `fontSize`，M3 的实现是把它盖到 `style`（默认 `bodyLarge`）上，`lineHeight` 仍继承主题的 24sp；而 `rememberScheduleHeaderMetrics` 用只写 `fontSize` 的裸 `TextStyle` 测量，量到的是字体自然行高。两行实需约 51dp，测量只算出 29dp，于是星期条定高 34dp。`Column` 把剩余高度传给下一个子项，日期那一行只剩 7dp 的纯行距，画了却没有一个像素。

- 抽出 `weekdayStyle(collapse)` / `weekdayDateStyle(collapse)`，`fontSize` 与 `lineHeight` 成对出现，**测量与渲染共用同一份**，从结构上消除再次脱节的可能。
- 两行实需 30dp（折叠态 26dp），仍落进原有行高，表头高度不变，不会把课表往下挤。
- 左侧月份格同样补上 `lineHeight`。
- `ScheduleAdaptationDeviceTest` 补两条断言：每个星期格必须有「星期 + 日期」两个语义子节点（否则 `onFirst`/`onLast` 同一个、比例恒为 1 会静默通过），且日期行高度不低于星期行的一半。这条以前无人看守，所以 `e6e5963` 那次「改实测行高」方向对了却没修上。

### 小组件：改为可滚动

原来按真实高度算行数再截断（`rowsAt` 末尾的 `.take(max)`），下午的课根本没进视图树。

- 「今日课程」「今日与明日」的列表改为 `LazyColumn`，两栏各一份。依据是 Glance 把集合按 `(appWidgetId, viewId, sizeInfo)` 缓存、`GlanceRemoteViewsService` 按 `EXTRA_VIEW_ID` 取数据，一个组件内多个滚动区有支撑。
- `WidgetCourseRow` 增加 `modifier` 参数：ListView 条目是独立的 `RemoteViews`，不继承根容器的点击，每行必须自己挂打开 App 的动作；行距也并进同一个 modifier，因为一个条目只能放一个可组合项。
- `skipEnded` 保留，已下课的课仍然隐藏，滑动看到的是接下来要上的课。
- 删除 `rowsThatFit()` 及其测试（周课表用的是 `weekGridRows`，不受影响）。

### 演示模式：开学日期兜底改为课表与小组件共用

`ScheduleRoute` 里「本周一」的兜底原先只喂给小组件快照，课表自己拿到的仍是 `null`。于是演示模式下桌面组件算得出周次，应用内的星期条却算不出日期与月份——这个不一致让日期行在预览包里根本无从验证。

- 抽出 `effectiveTimeBase`，小组件快照、当前周次推导、`ScheduleScreen(firstWeekDate = …)` 三处共用。
- 顺带修掉演示模式停在第 1 周、与真实周次对不上导致「今天」不高亮的问题。

### 验证

- 教务跳转：`:app:testDebugUnitTest` 417 通过；新增两条纯逻辑用例钉住 `zhjw1` 的真实跳转形状；另以临时探针跑通真实链路（`https://zhjw1.jju.edu.cn/jwglxt/` → 302 http → 升级 → 200 且含 `csrftoken`），探针文件未入库。
- 周视图日期行：**真机确认已显示**（vivo V2403A / Android 16 / OriginOS 6，演示包）。
- 小组件：`:widget:testDebugUnitTest` 28 通过，`:widget:compileDebugKotlin` 通过。桌面内可竖滑、「今日与明日」两栏各滑各的互不影响，均**已真机确认**（OriginOS 6）——一个组件内两个 `LazyColumn` 成立。仍未确认：点单行能否打开 App。

## [1.1.7] - 2026-09-19

本版本修复一个发版阻塞级缺陷：v1.1.6 的 `versionCode` 从 16 倒退为 6，导致应用内更新检测失效、且新版无法覆盖安装。

### 根因

`versionCode` 的生成方式与它的实际语义脱节，这次终于断裂。

- 1.0.x 时代 `versionCode` 恰好等于 patch 位（`1.0.74` → `74`），于是 `scripts/release.sh` 与 `release.yml` 都直接从版本号末段推算，看起来能用。
- 进入 1.1.x 后实际改成了独立递增序号（`1.1.3`→14、`1.1.4`→15、`1.1.5`→16），但两处推算逻辑没跟着改。
- `v1.1.6` 便算出 `6`，比上一版的 `16` 小 10。脚本原有的单调性检查没能拦住：它从**上一个 tag 名**推算基准值，而 `v1.1.5` 的 tag 名看着是 `5`，真实值却是 `16`，于是 `6 <= 5` 不成立，检查放行。

后果是连锁的：

- 应用内 `serverVersionCode(16) > localVersionCode(16)` 不成立，永远显示「已是最新」。
- Android 拒绝 `versionCode` 更低的包覆盖安装，用户即使手动下载 APK 也装不上。

### 修复

- **`versionCode` 改为语义化编码**：`major*10000 + minor*100 + patch`。版本号本身决定编号，不存在「忘记递增」的可能。
  - `1.1.6` → `10106`，`1.1.7` → `10107`，`1.2.0` → `10200`，`2.0.0` → `20000`
  - 天然单调递增，跨 minor/major 也安全；上界受 Android 的 `MAX_VERSION_CODE` 限制，major 到 2100 才会溢出。
  - 两个改动点保持一致：`scripts/release.sh`、`.github/workflows/release.yml` 的 `Resolve Version From Tag`。
- **单调性检查改为读取真实历史值**：基准取「历史上实际打出过的最大 `versionCode`」——从各 tag 的 `app/version.properties` 里读，不再从 tag 名推算。旧实现正是栽在这个假设上。
- **`docs/version.json` 纳入流水线**（新增 `Sync version.json to Pages` 步骤）：App 的 `UpdateManager` 读的是 GitHub Pages 这份，而 CI 此前只写 Gitee 那份，两份长期手工维护、各自漂移——事故发生时 Pages 还停在 `v1.1.5`。现在两份同源同值，`docs/version.json` 由 CI 提交回 `main`，Pages 自动重新部署。
  - Pages 那份的 `forceUpdate` 固定为 `false`：它是用户日常检查更新的入口，与 Gitee 那份「引导直连下载」的用途不同，强制弹窗会打断正常使用。
  - 提交时带 `pull --rebase` 重试（3 次），避免他人并发推送 `main` 时因竞态把整条流水线染红。

### 用户侧影响

- 已安装 v1.1.5（`versionCode` 16）的用户升级到 v1.1.7（`10107`）是正常的递增，可覆盖安装。
- 已安装 v1.1.6（`versionCode` 6）的用户同样能升到 `10107`。
- 从 v1.0.x 及更早版本升级的路径不受影响——`10074` 等旧值仍小于 `10107`。

### 验证

- 新编码算法本地实测：`1.0.74`→`10074`、`1.1.5`→`10105`、`1.1.6`→`10106`、`1.1.7`→`10107`、`1.2.0`→`10200`、`2.0.0`→`20000`，序列严格单调。
- `bash -n scripts/release.sh` 通过；`release.yml` 经 YAML 解析校验通过，新增步骤就位。
- 端到端以本版本发版本身作为验证：CI 全绿后核对 GitHub Release、Gitee Release、Gitee `version.json`、Pages `docs/version.json` 四处版本号一致。

## [1.1.6] - 2026-09-19

本版本为桌面小组件加入「提前预告」，并修好发版流水线中残留的上游仓库地址。

### 小组件提前预告

- 新增 `WidgetToday.nextDayWithCourses()`：从指定日期起往后最多找 7 天，返回第一个有课的日子；复用它已有的 `rowsAt()`，因此跨越教学周时周次会按目标日期重新计算，单双周课程不会串。
- 新增 `WidgetToday.hasCoursesOn()`：区分「某天本来有课、只是都上完了」与「那天真的没课」。组件此前用带 `skipEnded` 的课表判断，会把上完课的当天误判成没课并跳去预告次日。
- **今日课程**：今天有课 → 只显示今天的课，不预告；今天上完了 → 显示「今天的课都上完啦」，同样不预告；今天没课 → 显示下一个有课日，栏头写真实星期几。
- **近日课程**（原「今日与明日」）：左栏从今天起找第一个有课日，右栏从**左栏那天的后一天**继续找。两栏因此不会重复同一天，也不会因为中间夹着没课的日子而断掉；周末打开时左栏是周一、右栏是周三。
- 新增空状态文案「接下来一周都没课」：往后一周都找不到有课的日子（长假）时使用，不再复用「今天没有课啦」——那等于暗示只有今天空闲。
- 栏头不再写死「今天/明天」，改为按实际那天写 `DayNames`；周六打开时左栏装的是周一的课，写「今天」会让人误以为那几节课就在今天。
- 组件每次渲染重新读取当前时刻，不在组合期缓存：组件会长时间停在桌面，缓存时间会让跨天与课时段的判断失真。
- 新增 11 项回归测试（含跨周单双周、跳过空格日、7 天窗口边界、`skipEnded` 与预告的相互作用），`WidgetTodayTest` 由 13 项增至 24 项。

### 发版流水线修复

- `.github/workflows/release.yml` 与 `scripts/publish_announcement.py` 中 9 处指向上游仓库 `znj12345/zhengfang` 的硬编码地址改为本仓库 `sonder123i/capybara-course-helper`。此前 Gitee 同步、APK 上传与 `version.json` 回写都会打到上游仓库。
- 恢复 `b49afd3`（去除上游痕迹）中连同上游资源一并删除的发版脚本与工作流：`release.yml`、`scripts/release.sh`、`scripts/publish_announcement.py` 及其测试、Issue 模板。
- 同步修正 `test_publish_announcement.py` 中的下载地址夹具，使其与脚本的 `DOWNLOAD_ROOT` 一致。

### 验证

- `WidgetTodayTest` 24 项全部通过；`:app:compileDebugKotlin` 通过。
- `scripts/tests/test_publish_announcement.py` 15 项全部通过（此前因仓库地址不一致有 4 项失败）。

## [1.0.80] - 2026-09-16

本版本包含 v1.0.79 发布后的课表周数修复与学校服务域名调整。

### 课表周数

- 旧版保存的开学日期按所在周的周一读取，避免非周一日期被判为无效并回到第 1 周。
- 兼容只有节次时间的学期日历，以及早期尚未保存开学日期的迁移记录；补齐日期时保留已设置的节次时间、账号和学期隔离。
- 显示周数统一跟随当前学期日历，避免旧设置在页面恢复时覆盖已经浏览的周次。
- 区分日历请求的目标周与用户滑动后的周次回传，修复初始分页和旧回调覆盖目标周的问题；修改日期或切换学期时重新定位。
- 新增旧日历兼容、延迟日历加载、分页恢复、连续切周和日期更新的回归测试。

### 学校服务域名

- 学校适配申请、已适配学校目录和问卷接口统一使用 `https://school-api.hidisiwa.xyz`。
- 后台部署 Custom Domain，由 Cloudflare 管理域名解析和 HTTPS 证书；继续使用原服务、D1 数据库和管理员鉴权。
- 原 workers.dev 入口保留供尚未升级的客户端使用。

## [1.0.79] - 2026-09-16

本版本包含 v1.0.77 之后的全部应用改动。v1.0.78 发布流水线已在发布前取消，其问卷功能一并纳入本版本。

### 问卷中心

- 设置新增问卷中心，支持搜索、分类、推荐、进行中与已结束筛选，展示简介、发布方、预计用时、截止时间和封面。
- 收藏、浏览历史、手动填写标记和提醒记录按账号与学校隔离保存在本机，返回列表保留筛选和滚动位置；离线可查看缓存，开始填写前重新确认发布状态。
- 新增独立问卷 WebView，支持进度、返回、刷新、复制链接、系统分享、浏览器打开、文件选择及页面恢复，保留链接参数和正常认证跳转。
- 重要问卷每份提醒一次、每次进入应用最多提示一份，支持关闭轻提醒；问卷入口点击跟随匿名统计开关，不代表问卷星真实提交。
- 适配深色模式、关闭玻璃和减少动态效果，修正详情背景重叠及离线提示可见性；配套问卷发布、学校投放、定时管理和匿名点击统计后端已部署。

### 启动与网页登录

- 新增启动首屏偏好，可选择课程、课表、抢课、成绩和设置；首次进入按偏好显示，活动恢复仍保留当前页面。
- 教务网页登录增加网址输入、关键词搜索和返回学校入口，允许正常网页跨域跳转；导出的 Cookie 仍限定为所配置的教务地址。
- 强智新版登录改从公共入口获取表单，支持验证码提交、刷新和失败反馈，并保留表单实际提交地址与隐藏参数。
- 捕获教务自动识别与登录初始化阶段的异常，避免未选择适配器时中断登录流程。

### 选课与教学班队列

- 兼容正方首个分类写在 first* 隐藏字段的选课入口，补齐分类、轮次参数与显示名称，按教务脚本期望的字段构造列表和教学班请求。
- 展开教学班时优先复用课程行已有协议参数，减少重新加载入口、分类和课程列表的重复请求。
- 手动添加队列支持教学班名称，并结合教师、上课时间筛选；手动目标未匹配时不回退到其他教学班或旧缓存编号。
- 教学班条件纳入队列身份、持久化、复制、状态显示及去重，未填写教学班条件的旧队列保持原有行为。

### 会话稳定性与验证

- 尚未识别或未知的教务配置不直接创建适配器进行会话检查；初始化异常不误判会话失效，旧请求回调不影响新一轮检查。
- 补齐教务验证码、分类参数、网页登录导航、教学班匹配、启动页与会话检查测试，并纳入剩余工程配置和验证素材。
- 完整源码的本地 JVM 测试 396 项通过、3 项条件跳过，Debug 与 AndroidTest APK 编译通过，Lint 无错误；本次按要求不执行设备验证。

## [1.0.78] - 2026-09-16

> 发布流水线已在发布前取消，未发布安装包；本节内容并入 v1.0.79。

### 问卷中心与个人记录

- 在设置的“应用与支持”中新增问卷中心，支持搜索、分类、置顶推荐和进行中、已结束筛选，展示简介、发布方、预计用时、截止时间及可选封面。
- 新增收藏、浏览历史、手动“我已填写”、撤销标记和清空历史，按账号与学校隔离保存本机记录，返回列表保留筛选和滚动位置。
- 支持缓存时间展示和离线重试，进入填写前重新检查服务端发布状态；前台自动刷新间隔至少 5 分钟，不增加静置轮询。

### 应用内填写与提醒

- 新增独立问卷 WebView，支持进度、返回、刷新、复制链接、系统分享、浏览器打开、文件选择和页面恢复；保留链接参数及正常认证跳转。
- 设置入口显示未读红点，重要问卷每份提醒一次、每次进入应用最多提示一份；轻提醒可关闭，并避开已有登录、更新等提示。
- 更新匿名统计说明，问卷入口点击遵守统计开关，不作为问卷星真实提交统计。

### 界面与配套服务

- 复用现有玻璃卡片、子页面和筛选控件，适配深色模式、关闭玻璃及减少动态效果；详情页绘制独立壁纸背景，离线提示固定在筛选区下方。
- 配套后端已部署问卷发布管理、按学校投放、定时生效与截止、匿名点击去重和 CSV 导出功能，保留原学校适配服务。

### 验证

- 本地 Debug APK 与 AndroidTest APK 编译通过，Android JVM 测试 368 项通过、3 项条件跳过，其中问卷相关测试 15 项通过。
- 后端 TypeScript 检查和 48 项测试通过，生产 D1 迁移、公开接口与管理员鉴权已验证。

## [1.0.77] - 2026-09-15

### 登录与账号

- 新增河北传媒学院（hebic）默认学校配置，教务地址 `jwxt.hebic.cn`，系统类型 auto。
- 新增正方定制 CAS 统一身份认证（SSO）：`ZhengfangCasProtocol` 解析登录页，`ZhengfangCasSsoClient` 执行 CAS 流程，`ZhengfangRsaCrypto` 处理 RSA 密码加密；由 ZF 适配器自动探测，不按学校特判，同类正方 CAS 学校直接受益。
- 修复登录成功后提示"已登录但未识别到学号"的问题：学号识别增加 `sessionUserKey` 回退提取。
- `MatchingMemoryCookieJar` 放宽域名匹配，`ZjutSsoProtocol` 协议细节微调。

### 课表

- 抽取 `ScheduleCacheStore` 统一管理课表缓存，快照增加 `appliedCalendar` 字段。
- `ScheduleDates` 新增 `mondayOfWeek`，教学周按周一为起点计算。
- 学期开始日期变更自增 `revision`，设置页保存后立即生效；新增 `ScheduleCalendarFlowDeviceTest` 覆盖保存起始日期与系统返回键场景。

### 玻璃与动效

- 新增 `GlassLensCapture` 图层捕获与几何单测，`GlassLens`、`GlassLensRenderer`、`GlassLensShader` 采样与合成优化。
- 抢课页增加清晰背景层，控制台展开时才显示模糊；底栏、分段栏、液态控件动效细节调整。
- 新增玻璃捕获性能、页面入场、华为玻璃流程、模块阴影与连续性等设备测试。

### 验证

- 真实账号端到端冒烟：登录成功且学号识别正常。
- 全量 JVM 单测通过，Debug 与 Release APK 构建成功。
- 选课列表待学校开放选课阶段后由真实用户复核。

## [1.0.76] - 2026-09-13

### 玻璃显示与拖动

- 底栏、分段栏、数值滑块和颜色滑块直接使用指针位置及事件时间戳，点击与松手弹簧继承当前位置和速度；底栏拖回原标签也会准确归位。
- 分段栏切换阻尼 / 刚度调整为 `0.60 / 400`，松手为 `0.62 / 480`，按压为 `0.64 / 680`；按压缩放降至 `1.08`，速度拉伸上限降至 `0.13`。
- 修复 Android 12 的 Books 复合路径闭合后相对起点解析异常，保留 Phosphor 原图形和分层动画。
- API 31/32 折射按实测四角与当前坐标采样，修正圆弧菜单溢出和形变错位；对称轮廓保留简洁计算，没有色散时只采样一次。
- 开始按钮采样限制在周围区域，展开菜单时再扩大；大菜单的模糊光学底层最多回读 160,000 个像素。
- 底栏背景与图标分别缓存并直接合成，滚动和图标动画只采集变化的图层；图层录制完成后通知更新，结果位图不再被后续帧改写。
- 修正 API 33+ 玻璃色散的具名传参，避免误启用深度效果。

### 课表与课程详情

- 顶栏根据实际父容器宽度和文字测量自适应，放得下时保留原两列布局，窄屏和大字号再换行；星期与课程列统一偏移。
- 教室取消两行省略，统一节次高度随完整文字增加，保持时间轴、网格和课程跨度对齐；状态图标移到地点下方。
- 详情弹窗立即开始展开，主体约 220ms、内容约 240ms 完成，保留返回取消、主题切换、焦点恢复和删除撤销。

### 验证与已知限制

- 本地 JVM 317 项通过、3 条件跳过；Lint 无错误，Debug、Release、UiPreview 和 AndroidTest 编译通过。
- API 32 功能回归 44 项通过，API 35 复核 16 项通过；涵盖真实纹理折射、图标同步、反向拖动、长教室名、字号适配及实际操作录屏。
- 模拟器对照仍存在切页与滚动慢帧，本次不宣称整体帧率改善或卡顿完全解决。完整结果见 `docs/testing/2026-09-13-api32-fixes.md`。
- 本次归档覆盖 v1.0.75 之后的全部应用修正、回归测试及相关文档，不重复归档既有功能。

## [1.0.75] - 2026-09-12

### 四类教务与课程操作

- 接入新正方、旧正方、新强智、旧强智的统一协议层，读取学校实际返回的轮次、分类、课程、教学班及操作参数；保留原有新正方与统一认证兼容流程。
- 补齐新增系统的账密登录；旧正方支持标准 RSA 登录，两个旧系统支持图片验证码获取、刷新及错误重试，定制认证可回退网页登录。
- 对齐可选 / 已选课程、搜索筛选、单门与批量选退课、批量入队、手动队列、排序、精确目标、智能匹配和定时任务。未知容量显示“未公布”，跨轮次同名课程不会自动当成同一目标。
- 队列按账号保护轮次和表单状态，支持轮次等待、网络退避及停止条件；结果不明时核对已选课程，验证码、失效登录和人工确认会暂停后续提交。
- 统一课表、学期 / 总体成绩、学校返回的成绩分项与统计、考试安排、ICS / CSV 导出；旧正方历年成绩使用学校开放的按钮或学期选项，不编造缺失数据。
- 学校配置统一自动识别、四类教务选择、编辑兼容和支持说明；提供统一登录适配申请与历史入口。应用名称统一为“教务助手”。

### 会话恢复与状态隔离

- 统一会话版本校验、失效清理、自动与手动续期；切换账号或 Cookie 后丢弃旧请求回调，防止旧页面覆盖新账号数据。
- 隔离学校、账号、队列、目标、缓存及弹层状态；改进缓存提示、列表稳定身份、页面恢复和空闲渲染。
- 保留所有学校合计最多 3 个学生账号的规则，修正绑定与恢复边界；已保存账密的自动续期仍遵守学校验证码和人工确认要求。

### 课表、课程详情与提醒

- 自定义课程使用稳定身份，修复旧数据的缺失 / 重复 ID；支持课程编辑、非阻断冲突提示、删除及 5 秒撤销。
- 统一学期起始日期、周次和节次时间解析；周切换跟随手势并保留纵向位置，修复 ICS 周日偏移和课程文本转义。
- 增加默认关闭的课前 15 分钟提醒，按账号、学期和课程隔离；编辑、删除、同步、权限、开机及时间变化后重算，接收时复核课程与提醒状态。
- 课程详情按内容确定高度，上限为可用高度的 85%；时间与地点优先，窄屏、大字号及长地点纵向排列，教师和周次使用紧凑信息行。
- 自定义课程底部固定编辑，删除移入顶部更多菜单并等待完整退场后执行；详情从被点击卡片展开，支持内部滚动、拖动关闭、预测返回取消及焦点恢复。

### 玻璃界面、主题与动效

- 抢课工作台改用 64dp 玻璃圆钮和单层圆弧菜单，支持长按原地松手、拖动预选、松手执行和取消；开始按钮采样壁纸与实际页面内容，降低表面遮罩，使背景折射可见。
- 五个底栏图标采用 Phosphor 线面分层设计，选中后播放一次，重复点击当前项仅重播图形；可见层、玻璃采样层和收起胶囊共用播放状态，保留 MIT 许可。
- 分段控件增强按压、速度形变及弹簧回弹；连续切换、反向拖动和尺寸变化继承当前状态，折射、高光和轮廓使用同一形变。
- 五个主页共用分组入场时间线，滚动和刷新不重播；菜单、展开区域与详情在退场后移除或执行操作，减少重复的大幅位移。
- 新增跟随系统 / 浅色 / 深色主题，联动原生窗口、WebView、系统栏及玻璃缓存；保留自定义壁纸参数，修复课表和成绩顶栏的白雾、接缝及下拉玻璃显示。
- 改进启动页与应用内 Logo 的衔接、顶栏触区、筛选锚点和窄屏 / 大字号可达性；系统关闭动画时直接呈现最终状态。

### 匿名统计与工程

- 增加可关闭的匿名安装统计，首次展示说明；仅上报随机安装标识和版本，按北京时间每日成功一次，调试 / 演示构建不计数，标识不参与系统备份。
- 新增 Cloudflare Workers、D1 与 Access 管理页面，展示累计设备、今日活跃和近 30 天活跃；管理访问使用邮箱验证和服务端 JWT 校验。
- 增加 UiPreview / benchmark 构建、协议样本、单元回归、设备操作测试与验证文档。发布前本地 JVM 312 项通过、3 项授权环境用例跳过；MuMu 去重后 62 项通过、1 项条件跳过。
- 发布流程增加构建前检查和公告同步：校验更新日志与公告文件，在 APK 和版本信息发布完成后追加本次公告，保留历史，失败时让流水线明确报错。

### 发布流程修复

- v1.0.74 在发布前检查阶段失败，未产出安装包或 Release；本版继续包含上次已打包版本 v1.0.73 之后的全部 v2 更新。
- 修复中断响应重试测试在 Linux 上的双栈路由问题：MockWebServer 与请求统一使用 IPv4 回环地址，避免 localhost 重试切换到未监听的 IPv6 地址。保留只读请求重试成功及写入请求不重放的断言，业务重试策略不变。
- 发布检查无论成功或失败都上传 JVM 和 Lint 报告，便于直接查看完整异常与测试结果。

### 本次归档范围

以上内容覆盖上次已打包版本 `v1.0.73` 之后的全部提交：

| 提交 | 归档内容 |
| --- | --- |
| `11ed699` | v1.0.73 公告存档，保留历史，不重复计为本版新功能 |
| `5c719f9` | 四类教务协议层、登录与选课链路、查询导出、玻璃 UI 基础与测试 |
| `10b7934` | 会话恢复、请求版本校验、页面与账号状态隔离 |
| `e55735c` | 课表编辑与提醒、主题与启动、学校配置、圆弧菜单和匿名统计 |
| `2dd8f2f` | 开始按钮折射、Phosphor 图标、分段弹性、分组动效和课程详情 |
| `4bdeed5` | 发布前检查、公告校验与 Gitee 公告同步 |
| `7cd5972` | 未发布的 v1.0.74 版本与更新日志归档 |
| `dd57701` | 固定测试回环地址、保留 CI 检查报告与 v1.0.75 公告归档 |

真实学校验证覆盖登录与只读查询，没有执行真实选课或退课。API 31/32 使用的 GLES 路径已测试，本轮没有该系统版本的整机实测记录。

## [1.0.74] - 2026-09-12（未发布）

发布前的 JVM 测试因 Linux localhost 双栈路由选择失败，本版本未产出安装包或 Release。已推送的标签保留，以下功能与测试修复一并顺延至 v1.0.75。

### 四类教务与课程操作

- 接入新正方、旧正方、新强智、旧强智的统一协议层，读取学校实际返回的轮次、分类、课程、教学班及操作参数；保留原有新正方与统一认证兼容流程。
- 补齐新增系统的账密登录；旧正方支持标准 RSA 登录，两个旧系统支持图片验证码获取、刷新及错误重试，定制认证可回退网页登录。
- 对齐可选 / 已选课程、搜索筛选、单门与批量选退课、批量入队、手动队列、排序、精确目标、智能匹配和定时任务。未知容量显示“未公布”，跨轮次同名课程不会自动当成同一目标。
- 队列按账号保护轮次和表单状态，支持轮次等待、网络退避及停止条件；结果不明时核对已选课程，验证码、失效登录和人工确认会暂停后续提交。
- 统一课表、学期 / 总体成绩、学校返回的成绩分项与统计、考试安排、ICS / CSV 导出；旧正方历年成绩使用学校开放的按钮或学期选项，不编造缺失数据。
- 学校配置统一自动识别、四类教务选择、编辑兼容和支持说明；提供统一登录适配申请与历史入口。应用名称统一为“教务助手”。

### 会话恢复与状态隔离

- 统一会话版本校验、失效清理、自动与手动续期；切换账号或 Cookie 后丢弃旧请求回调，防止旧页面覆盖新账号数据。
- 隔离学校、账号、队列、目标、缓存及弹层状态；改进缓存提示、列表稳定身份、页面恢复和空闲渲染。
- 保留所有学校合计最多 3 个学生账号的规则，修正绑定与恢复边界；已保存账密的自动续期仍遵守学校验证码和人工确认要求。

### 课表、课程详情与提醒

- 自定义课程使用稳定身份，修复旧数据的缺失 / 重复 ID；支持课程编辑、非阻断冲突提示、删除及 5 秒撤销。
- 统一学期起始日期、周次和节次时间解析；周切换跟随手势并保留纵向位置，修复 ICS 周日偏移和课程文本转义。
- 增加默认关闭的课前 15 分钟提醒，按账号、学期和课程隔离；编辑、删除、同步、权限、开机及时间变化后重算，接收时复核课程与提醒状态。
- 课程详情按内容确定高度，上限为可用高度的 85%；时间与地点优先，窄屏、大字号及长地点纵向排列，教师和周次使用紧凑信息行。
- 自定义课程底部固定编辑，删除移入顶部更多菜单并等待完整退场后执行；详情从被点击卡片展开，支持内部滚动、拖动关闭、预测返回取消及焦点恢复。

### 玻璃界面、主题与动效

- 抢课工作台改用 64dp 玻璃圆钮和单层圆弧菜单，支持长按原地松手、拖动预选、松手执行和取消；开始按钮采样壁纸与实际页面内容，降低表面遮罩，使背景折射可见。
- 五个底栏图标采用 Phosphor 线面分层设计，选中后播放一次，重复点击当前项仅重播图形；可见层、玻璃采样层和收起胶囊共用播放状态，保留 MIT 许可。
- 分段控件增强按压、速度形变及弹簧回弹；连续切换、反向拖动和尺寸变化继承当前状态，折射、高光和轮廓使用同一形变。
- 五个主页共用分组入场时间线，滚动和刷新不重播；菜单、展开区域与详情在退场后移除或执行操作，减少重复的大幅位移。
- 新增跟随系统 / 浅色 / 深色主题，联动原生窗口、WebView、系统栏及玻璃缓存；保留自定义壁纸参数，修复课表和成绩顶栏的白雾、接缝及下拉玻璃显示。
- 改进启动页与应用内 Logo 的衔接、顶栏触区、筛选锚点和窄屏 / 大字号可达性；系统关闭动画时直接呈现最终状态。

### 匿名统计与工程

- 增加可关闭的匿名安装统计，首次展示说明；仅上报随机安装标识和版本，按北京时间每日成功一次，调试 / 演示构建不计数，标识不参与系统备份。
- 新增 Cloudflare Workers、D1 与 Access 管理页面，展示累计设备、今日活跃和近 30 天活跃；管理访问使用邮箱验证和服务端 JWT 校验。
- 增加 UiPreview / benchmark 构建、协议样本、单元回归、设备操作测试与验证文档。发布前本地 JVM 312 项通过、3 项授权环境用例跳过；MuMu 去重后 62 项通过、1 项条件跳过。
- 发布流程增加构建前检查和公告同步：校验更新日志与公告文件，在 APK 和版本信息发布完成后追加本次公告，保留历史，失败时让流水线明确报错。

### 本次归档范围

以上内容覆盖上次已打包版本 `v1.0.73` 之后的全部提交：

| 提交 | 归档内容 |
| --- | --- |
| `11ed699` | v1.0.73 公告存档，保留历史，不重复计为本版新功能 |
| `5c719f9` | 四类教务协议层、登录与选课链路、查询导出、玻璃 UI 基础与测试 |
| `10b7934` | 会话恢复、请求版本校验、页面与账号状态隔离 |
| `e55735c` | 课表编辑与提醒、主题与启动、学校配置、圆弧菜单和匿名统计 |
| `2dd8f2f` | 开始按钮折射、Phosphor 图标、分段弹性、分组动效和课程详情 |

真实学校验证覆盖登录与只读查询，没有执行真实选课或退课。API 31/32 使用的 GLES 路径已测试，本轮没有该系统版本的整机实测记录。

## [1.0.73] - 2026-09-03

### 正方 V9 适配（xkkz_xh）

- **新增 `CourseNameKit`**：两组能力——全半角括号归一化（`normalizeBrackets`）与 xkkz 参数名自适应（`detectXkkzKey` / `resolveIndexXkkz`）。正方教务存在两套选课参数名：旧版 `xkkz_id`（Index 页 hidden input 为 `firstXkkzId`），V9（如 mnust）用 `xkkz_xh`（`firstXkkzXh`）。判定按值优先，避免 Display 响应的空字段误判；均无值时回落 `xkkz_id`，既有学校行为不变。
- **选课全链路参数名自适应**：课程列表（CourseListRoute）、手动/智能选课（GrabService、SmartSelector）的列表、选课、抢课与搜索表单按 `detectXkkzKey` 决定主键名，V9 学校同时携带 `xkkz_xh`。Display 参数请求新增 `fetchCourseDisplayParamsWithKey` / `fetchCourseDisplayParamsSyncWithKey`（Java 无法用重载区分同名 String 参数，故独立方法名）。
- **解析兼容**：CourseParser 与选课详情解析在 `xkkz_id` 为空时回落读取 `xkkz_xh`。

### 课程名全半角括号归一化

- 教务库课程名通常为半角括号（"大学体育(三)"），中文输入法默认输出全角（"大学体育（三）"），直接字符串比较会匹配失败。列表搜索、课程过滤、抢课本地匹配（含权重评分）统一走 `normalizeBrackets` 归一化后再比较，仅用于比较，不改变原始存储。

### 其他

- **自定义学校域名支持端口**（如 `zfjw.mnust.edu.cn:30443`）：登录页与添加学校弹窗的域名校验正则允许 `:端口` 后缀，输入框增加提示文案。
- **JVM 单测**：`testOptions.unitTests.returnDefaultValues = true`，被测类内部的 `android.util.Log` 在 JVM 上返回默认值而非抛异常；新增 `CourseNameKitTest` 覆盖括号归一化与参数名自适应，全量 `testDebugUnitTest` 通过。

## [1.0.71] - 2026-08-31

### API 31/32 的圆钮折射

- **开关、滑块与取色器的圆钮接上离屏折射**：v1.0.70 覆盖了按钮、芯片、分段控件、选择器与弹窗，但这三类「圆钮压在自己轨道上」的控件走的仍是库的 `lens()` 直接调用——它在 31/32 上第一行就返回，实际只剩 `blur(8dp * (1 - press))`。App 级锚点的底图只有壁纸，而这三类圆钮在 33+ 上采的是 `combined(环境背景, 轨道层)`，折射里该浮出来的**正是脚下轨道的颜色**（开关的绿、蒙版条的黑白、色板的色相），所以各自建区域，底图 = 环境 + 自己的轨道，锚点挂在**不随圆钮移动**的外层容器上。共用参数收进新增的 `ThumbLens.kt`：材质、光学、形变三个函数，开关/滑块/色板只差 `lens()` 的两个 dp 参数与速度除数。
- **纯色轨道必须把 0.75 缩放烤进底图**：开关的轨道是纯色，平场折射出来还是平场——实测底图不缩时按住开关，整个开关区域是均匀的 `0fd840`，旋钮彻底消失，屏幕上只剩一圈 highlight（用户报的就是这个）。33+ 那边之所以有东西可看，全靠 `scale(0.75, 0.75)` 把轨道缩得**比旋钮还矮**（64×28dp 缩成 48×21dp，而旋钮是 40×24dp）：旋钮上下边缘于是采到轨道**外面**的环境背景，绿/环境那条边才是折射真正在弯的东西。烤的是按满那一档（静止时折射为 0、表面是不透明实色，底图看不见；只有按满才可见），缩放原点是**旋钮中心**、与库一致——绕锚点中心缩会让旋钮在 checked 那端探出轨道右缘 6dp。色板不烤：150dp 连续渐变缩不缩都有内容可折射，只是放大率略有不同；而摘钮中心跟 (saturation, brightness) 每帧动，要跟就得每帧重拍（一次约 5ms），为一点放大率差不值得。
- **vibrancy 与调用点对齐**：`GlassLensOptics.vibrancy` 此前统一 1.28，但开关、滑块 thumb、取色摘钮的 33+ effects 里只有 `blur` 与 `lens`、**没有** `vibrancy()`。实测：API 32 开关按住折射出 `16d245`，API 37 同一处是 `34c759`（= 轨道原色）——纯色轨道折射出来本该还是同一个绿，1.28 把它推得更饱和更暗。现在 `thumbLensOptics` 固定传 1，底栏/分段控件那些真调了 `vibrancy()` 的站点仍是默认 1.28。
- **按压 + 速度形变收敛到唯一算式**：`thumbLensTransform` 是唯一实现，库那层 `drawBackdrop(layerBlock)` 与 API 31/32 的 `glassLens(scale)` 读同一个函数。两处各写一份的后果在底栏指示器上出过一次：按下时 highlight 环按放大后的轮廓走、折射还是原尺寸，屏幕上是一圈白环浮在玻璃外面。`reduceMotion` 分支也随之只剩一处。
- **拖动期间限频重拍**：蒙版/模糊滑块一拖，改的就是壁纸本身，底图不重拍的话折射里是拖动前的壁纸。走 `GlassLensFreshness` 限频（100ms 一次），松手补一张——用户真正盯着看的是静止态。开关拨动类似：轨道色是 `lerp(灰, 绿, value)` 的动画，底图只 key 了 `checked`、拍到的是动画起点的颜色，约 300ms 后补拍一张。

### 修复

- **换壁纸后折射取样旧壁纸**：换壁纸必须重拍底图，这一条原先是各调用点自己的 key——MainActivity 的两个区域写了 `AppearanceSettingsManager.style`，**底栏和分段控件没写**，漏的那两个在换壁纸后永远折射着旧壁纸。实测（API 32，「背景」弹窗切预置/颜色/图片三段）底图每次都等于上一张屏幕：与上一张的差 6.2 / 10.8 / 6.6，与当前的差 42.7 / 47.6 / 18.7，屏幕上就是用户报的「切换的时候会取样上一个」。现在这条**建在 `rememberGlassLensAnchor` 里，不交给调用方**：`snapshotFlow` 盯壁纸样式，且在 flow 里读而不是组合期读（组合期读会让每个带折射的子树都订阅壁纸）；立刻拍一张，240ms 后再补一张——`version++` 之后下一次 draw 才会重拍，而那一帧壁纸层可能还没按新样式重录，图片壁纸的位图更是异步解码的；`collectLatest` 让连续切换只有最后一次走到底。MainActivity 两个调用点的 `AppearanceSettingsManager.style` key 随之移除。

## [1.0.70] - 2026-08-30

### API 31/32 的真折射

- **用离屏 OpenGL ES 2.0 补上缺失的着色器能力**：`RuntimeShader`（AGSL）是 API 33 的平台类且没有兼容库，因此库的 `lens()` 在 31/32 上第一行就 `return`，这两个版本此前只有模糊。GLSL ES 2.0 自 API 8 就有，所以改走离屏 GL：EGL pbuffer 上下文 + FBO 渲染 + `glReadPixels` 回读，再由 `DrawModifierNode` 上屏。着色器是 kyant `RoundedRectRefractionWithDispersion` 的逐行同式移植（SDF、解析梯度、`circleMap` 曲线、七波长权重表、`(cx*cy)/(hx*hy)` 位置调制、位移取负配朝外梯度实现朝内采样），不是另做一套效果。设计与踩过的坑见 `docs/glass-lens-api32.md`。
- **覆盖所有玻璃控件**，不只底栏：按钮与圆钮、芯片、开关、分段控件、选择器下拉、弹窗面板。
- **真色散替换伪造的彩边**：此前 31/32 走的是 `chromaticFringe`，即在边缘画一圈固定的蓝黄描边。现在由着色器按七个波长分别采样合成，可见的彩边来自相邻波长的权重差；三通道 ±push 做不出这个效果，那只是 RGB 错位。三个通道的权重各自和为 1，因此均匀场进 = 均匀场出，不会整体染色。
- **`hasRealLens` 与 `hasLensLook` 分开**：前者回答"平台有没有 AGSL"，只该用来门控 `lens()` 调用；后者回答"这个站点看起来有没有折射"。额外模糊、更低的表面透明度、缺失的描边这些**补偿**只因为"这台设备没有折射"才存在，判据必须换成后者，否则折射会叠在补偿之上。
- **底图按区域共享、渲染目标按元素独占**：底图上传一次约 5ms，是贵的那一头，所以挂在不随控件移动的祖先节点上，控件滑动时只改采样窗口。但 FBO 与输出位图必须每元素独占 —— 曾经两者在同一个类里，改成 App 级共享锚点后所有元素往同一个 FBO 提交、又都读同一张输出，每个元素画出来的是最后渲染完的那个元素的结果拉伸到自己尺寸（屏幕上是选择器胶囊里一条灰带，140 行像素逐行几乎相同）。
- **底图必须复现控件实际压着的画面**：AGSL 的输入由平台喂，GL 这边要手工重建。底栏指示器压着"模糊壁纸 + 轨道容器色 + 锐利 tint 文字"；成绩页芯片压着 `combined(壁纸, 顶栏玻璃层)`，无模糊无轨道色；弹窗与下拉走 `vibrancy → blur → lens`，那层模糊只能**烤进底图**，因为屏幕上的 blur 加在 `drawBackdrop` 建的图层里，而折射在它上游就画完了。抄邻居的配方会得到一张错的底图。
- **按需重拍底图**：内容变化立刻重拍，滚动期间限频 100ms 一次，滚动停下补一次，静止零开销。试过并放弃常驻 `HardwareRenderer` + `SurfaceTexture` 的零回读通路：中位 4.69ms、p90 15.99ms，反而更差，因为它按 vsync 节拍走。

### 修复

- **底栏指示器静止时的边缘彩边**：`NavIndicatorRefractionFloor` 曾是 `0.42`，即静止也保留四成折射，注释写着"凸起镜片感"并当成 API 33+ 的既有基线。查库源码后确认库的指示器是 `lens(10dp * pressProgress, 14dp * pressProgress)`，静止**恰好为 0**、没有任何下限。这个自造的下限在 31/32 上后果更重：那条路采模糊底图，页面的彩色内容被摊成大色斑，静止折射把色斑沿胶囊边缘挤一圈——课表、成绩、设置三页底部有彩色内容，所以只有它们看得见。改为 `0f` 后实测斜坡与位移在静止态均为 0（原 15.1 / 22.7），按下仍到 `amount=54.35 thick=36.17`。两条路读同一个常量，因此 API 33+ 的指示器静止态同样不再折射，与库一致。注意库的**轨道**是无条件 `lens(24dp, 24dp)`，静止是折射的，这一点未变。
- **底栏指示器换页后底图不更新**：换页重拍那段是死代码。`selectedTab` 是普通 `Int` 形参而非 snapshot state，`snapshotFlow { selectedTab }` 只会发出启动该 effect 时捕获的那个值，此后再不发出；而 effect 的 key 里也没有它，所以永不重启。实测点标签后 logcat 里一条重拍记录都没有。改为把 `selectedTab` 当 key，顺带获得"连续换页时前一次的延迟被取消"的语义。另外重拍要等入场动画（160ms）结束后再补一次，否则拍到的是动画中途那一帧。
- **弹窗面板整块消失**：折射路径下调用方的 `onDrawBackdrop` 会让掉正常的背景绘制（背景由折射负责画），所以一旦锚点没被 `Modifier.glassLensAnchor` 挂到节点上，折射一个像素都不画、背景也没人画。现在两张底图都会挂上，`failed` 改用 snapshot state 存（失败会触发重组、连模糊兜底一起切回去），并且锚点未挂载时会明确报错而不是静默出错。
- **弹窗内部出现巨大圆弧**：`lensCornerRadiusPx` 只认 `CornerBasedShape`，而弹窗用的是 kyant `RoundedRectangle`，于是回落到"短边一半"约 295px，SDF 退化成胶囊。补上 `RoundedRectangularShape.corners()` 分支（与库自己的 `lens()` 读同一个接口），并加了回归测试。

### 工程

- **移除临时诊断脚手架**：`GlassLensProbe` 及其全部调用点。它此前在 `MainActivity` 里无条件开启、没有 `BuildConfig.DEBUG` 守卫，若随版本发出去会让每台设备把屏幕快照 PNG 写进 `cacheDir`。
- **新增仪器测试**：`GlassLensRendererTest` 在真机/模拟器上验证离屏折射真的出图，覆盖形状外透明、边缘带位移、色散只在圆角、多元素共享一个 GL 上下文、释放其中一个不影响其余、以及高频提交不堆积。另三个用例记录探路阶段的结论：`Bitmap.createBitmap(Picture)` 可用、ES 2.0 离屏能跑复杂片元着色器、零回读通路更慢所以放弃。
- **新增文档** `docs/glass-lens-api32.md`：说明哪些是从库移植的、哪些是本项目自己写的，以及三个查了很久的坑（位移被自己 clamp 到库的 1/3、着色器里多画一份轮廓光、自造的静止折射下限）。

## [1.0.69] - 2026-08-19

### 玻璃与壁纸

- **平滑背景上的折射**：折射是位移采样——弯曲一片均匀颜色，采回来还是同一个颜色。壁纸原本只有底色加几个大半径径向渐变，空间频率过低，因此任何折射强度都无法显形。现在壁纸叠一层均值中性的微纹理（等宽等透明度的白线与黑线错开半个周期，平均明度不变，读起来是布纹而非灰雾），缓存为 12px 平铺 ImageShader，每帧只有一次 `drawRect`。
- **debug 与 release 观感对齐**：`debugPiracyWatermark` 原先画在被采样的壁纸图层**内部**，且只在 `FLAG_DEBUGGABLE` 下存在，等于给折射准备了一张高对比标靶——测试包因此永远显得正常，而正式包暴露真实情况。它是全应用唯一一处按构建类型分叉的渲染路径，现已移到所有捕获层之外的最上层，测试包从此可以用来判断这类问题。
- **微纹理只对不模糊的玻璃层有效**：底栏轨道是 `blur(10dp)` 之后才 `lens()`，30px 模糊会先把 12px 纹理抹平。收益集中在 `blurDp = 0` 的层——底栏选中胶囊、顶栏芯片、开关滑块、分段滑块。两端都已注明，不靠猜。
- **液态玻璃总开关**：设置 → 外观新增开关。关闭后落到 `GlassCapability.Material`，走各组件**已有的** API 31 以下回退路径，因此不需要任何新的渲染代码。
- **修复图片壁纸消失**：无玻璃分支原先只填 `baseColor`，于是关掉玻璃或在 API 31 以下时，导入的图片壁纸会整张不见、只剩一块纯色。

### 稳定性

- **透镜形状闸门改为真实类型判定**：`lens()` 对不支持的形状会在**绘制阶段**抛 `UnsupportedOperationException`，而 `LiquidPicker` 的液滴融合轮廓正是 `GenericShape`，依赖这道闸门。判定从按类名反射改为 `is RoundedRectangularShape || is CornerBasedShape`，并改为从 `BackdropEffectScope` 读形状——闸门与 `lens()` 实际使用的形状因此不可能再脱钩。
- **清理误导性死代码**：删除 `SpectralRim.kt` 与 `GlassOpticsSpec` 的 `spectralRimAlpha` / `specularAlpha` / `innerGlowAlpha`。`Modifier.spectralRim` 没有任何调用点，且在所有开启玻璃的 API 31+ 设备上第一行就返回，那三个数字只是看着像旋钮。

### 发布流程

- **版本号从 tag 推导**：`app/version.properties` 不再是权威，CI 从 tag 反写，「版本与 tag 不一致」这类失败在结构上不再可能。versionCode 仍取 patch 位，与既有编号连续，并新增单调递增校验。
- **前置校验提前**：tag 格式、更新日志、版本号三项校验全部移到 `assembleRelease` 之前，失败从 5 分钟缩短到 30 秒。
- **一条命令发版**：新增 `scripts/release.sh X.Y.Z`，一次完成版本号写入、更新日志骨架与校验（与 CI 同一条解析管线）、CHANGELOG 归档、提交与打 tag。

## [1.0.68] - 2026-08-18

### 界面与交互

- **液态玻璃体系重做**：按钮、开关、分段控件、选择器统一到同一套光学管线（折射 + 色散 + 镜面高光），按压形变改为各向异性挤压配合弹簧回弹；相邻控件在按压靠近时会以 SDF 平滑并合，松手后分离。
- **顶栏随滚动收起**：课表与成绩页的顶栏改为可折叠玻璃条，折叠行程严格等于两态高度差，因此收缩与滚动 1:1 对消、全程跟手。
- **筛选面板改为浮层**：不再在布局流中展开，列表滚动位置得以保留，面板本身折射其后的课程卡片。
- **浮动通知**：页内提示改为单个元素在胶囊与长条之间连续形变，顶栏会上报自身底边，通知不会压住顶栏按钮。

### 壁纸与外观

- **自定义壁纸**：支持预置背景、纯色与本机图片三种来源，统一走同一条渲染路径；导入时按 EXIF 方向校正并降采样重编码。
- **模糊与暗化**：图片壁纸可调模糊与暗化。模糊用三遍 box blur 近似高斯，因此在 API 24 上同样可用，不依赖 RenderEffect。
- **随壁纸自适应**：顶栏文字与玻璃控件的深浅依据壁纸对应区域的 Rec.709 亮度决定，并带迟滞阈值防止边界抖动。

### 登录与账号

- **浙江工业大学统一身份认证**：新增 CAS 三阶段登录（登录页 → RSA 加密提交 → 服务票据重定向链），处理验证码开关与 flow-execution 重试。
- **凭据加密留存**：密码经 AndroidKeyStore AES/GCM 加密后保存在本机；任何失败一律降级为空，不会崩溃。
- **静默续期**：会话过期时先尝试单飞续期，成功则继续，失败有 60 秒冷却；Cookie 看护不再在首次过期后永久停止。

### 屏幕适配

- **连续系数替代尺寸分档**：新增 `ScreenMetrics`，以纵向/横向紧凑度两个连续系数插值原有写死几何。两个系数在 20:9 上恰好为 0，因此主力比例观感零变化。
- **底栏几何收敛到单一来源**：轨道高度与内容留白此前各算一套（底栏 `8+72+8+navBar`，各页写死 96dp），三键导航下列表末项有 16dp 被遮挡。现统一由 `NavBarMetrics` 导出。
- **弹窗安全区**：安全区改在宿主层施加。卡片本身无限高且部分正文为 `heightIn(max = 420.dp)`，此前在 640dp 屏幕上确认按钮会落到导航栏之下。

### 性能

- 玻璃渲染按运行时能力分档，低能力设备回退到透镜采样而非实时模糊；标签切换与页面转场的合成层数量下降。

### 问题修复 (Bug Fixes)

- 自定义壁纸选图：`inJustDecodeBounds` 按约定返回 null 曾被误判为打开失败，导致所有图片导入失败。
- 开关拖动与外部状态互斥导致的竞态；分段控件在顶栏折叠期间因宽度逐帧变化而丢失拖动状态。
- 拖动饱和按位移模长计算，消除对角方向 1.41 倍过冲造成的八向摇杆感。

## [1.0.35] - 2026-05-26

### 🔧 部署与流水线 (CI/CD)
- **支持手动构建**：在 GitHub Actions 的 `release.yml` 工作流配置中添加了 `workflow_dispatch` 选项。若因 GitHub 服务器事件丢失或其它原因未自动触发 Tag 动作，用户也可以在 Actions 控制台界面手动触发编译并发布 APK。

## [1.0.34] - 2026-05-26

### 🐛 问题修复 (Bug Fixes)
- **退课失败修复**：修复了由于使用未加密的原始 ID `classId` 提交退课请求导致后台拒绝操作的问题。现在已选退课接口已优先正确传递加密过的 `doJxbId` 选课标识。
- **课表分享崩溃修复**：修复了在导出并分享 `.ics` 课表文件时，由于共享文件处于缓存根目录而未在 `file_paths.xml` 中配置相应映射，导致 `FileProvider` 抛出 URI 异常引发的崩溃错误。

## [1.0.33] - 2026-05-26

### ✨ 体验优化 (UI/UX)
- **非线性展开动效**：将课程折叠列表的展开/折叠以及箭头的旋转动画，统一重构为高级非线性 Material 3 Emphasized 物理曲线（`MotionSpecs.emphasized`），大幅拉长了淡出和减速阶段的曲线尾流，使得界面在视觉上呈现极高的高级感与顺滑感。

## [1.0.32] - 2026-05-26

### ✨ 体验优化 (UI/UX)
- **已选界面重构**：重写了 `SelectedCoursesScreen`，背景换用系统级 `NeuSurface`，卡片重构为带阴影和光影效果的 `SystemCard`，且各项信息配以更直观的矢量图标。
- **对话框动画统一**：已选界面的“确认退课”对话框改用平滑的底部滑入渐显（`slideInVertically`）和轻柔渐隐（`fadeOut`）的过渡动画，与设置、课程详情等弹窗风格保持一致，弃用原有僵硬的系统 AlertDialog。
- **课程列表展开动画**：将课程列表的展开/折叠过渡动画从带有轻微弹跳的弹簧动画（`gentle`）调整为经典流畅的 `tween(300)` 配合 `FastOutSlowInEasing` 缓动，兼顾交互效率与流畅感。
- **课表空间与防遮挡**：微调了课表左右侧的时间列宽度（34.dp -> 28.dp），缩减卡片内边距至最紧凑水平，并还原了合理的字体阶梯，支持最多 6 行的文字展现，彻底解决了变宽后字符在窄屏幕下被裁剪的问题。

### 🐛 问题修复 (Bug Fixes)
- 修复了此前在已选课程页面执行退课操作后，列表缓存没有同步刷新，导致切回主页面列表状态不正确的持久化同步漏洞。

## [1.0.31] - 2026-05-26

### ✨ 体验优化 (UI/UX)
- **视觉重构**：优化所有按钮与分段控制器的文本垂直居中问题，移除强制裁切和人工位移，恢复 Android 原生完美对齐。
- **克制拟态**：精简基础按钮（主要/次要/危险按钮）的重度液态玻璃阴影，降低视觉负担，提升层级清晰度。
- **监控模块**：重构 `GrabPro` 界面监控目标的显示，解除了长课程名称被强行压缩进 StatusBadge 的限制，改用加大加粗的独立标题展示。
- **课程列表**：将课程列表项改为轻量级的玻璃卡片风格，并新增了授课教师和课程容量的精美图标指示（Person/Group），提升页面饱满度。
- **课表增强**：解除了原先课表格子内的行数限制和缩写强制（如不再强制截断校区），确保完整的课程名称与上课地点完整显示。
- **弹簧动效**：
  - 课表视图 (Timetable)：为课程色块增加了响应式物理按压缩放动画，手感更加真实 Q 弹。
  - 课程展开 (CourseList)：课程组的展开/收起过渡从僵硬的线性动画替换为了 `MotionSpring.gentle()` 弹簧阻尼引擎，并加入了平滑的 180 度箭头翻转动画，顺畅自然。

### 🐛 问题修复 (Bug Fixes)
- 修复部分设备上中文渲染因 `includeFontPadding = false` 导致的文字整体偏上问题。
