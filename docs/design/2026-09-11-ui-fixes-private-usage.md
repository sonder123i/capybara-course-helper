# 教务助手：界面修复与私有统计

承接会话 `01a0906b-1d35-7720-9d28-3c80e040f9f6` 中已确认的计划，在现有工作区继续实现，保留包名、签名、账号数据及液态动效。

- 抢课入口使用直径 64dp 的玻璃圆钮，采样背景并保留折射，中央用实心播放／停止图标，展开时变为收起图标；固定于右下方（右 16dp、安全区上方 12dp）。长按向左上展开 90 度扇形玻璃底板，六个 48dp 操作按钮沿约 188dp 半径的同一条圆弧排列；按水平、垂直触区间距调整角度，在窄屏保持紧凑且互不重叠。页面内容随展开进度模糊，并叠加轻微暗色遮罩。低版本及关闭玻璃效果时使用实体材质回退。
- 按钮和底板从原点弹性展开并回收；SVG 分层描画配合课程卡片展开、扫描、时钟指针、参数滑块、日志和队列动效。拖动预选、松手执行，原地松手保留菜单供点选；选中放大、按压回弹和轻振反馈，提示位于扇形上方，不遮挡操作。返回、外部点击或取消手势关闭。减少动态效果时直接切换，保留所有操作。
- 修复自定义图片下课表、成绩顶部的浅色边框及白雾：普通显示模式不再添加整块 90% 浅色遮罩，保留收起时的局部玻璃；系统状态栏的明暗判断与实际遮罩一致。
- 恢复下拉选择器原有玻璃高光、折射及投影，仅去除重复的临时轮廓。
- 共用下拉组件消除重复边缘；等待回收完成并移除浮层后再执行“添加学校”，离开页面即取消。
- 添加、编辑学校共用自动识别及四类教务选项；结构化保存地址和类型，保留已有兼容配置。
- 当前应用名称统一为“教务助手”，教务名称、支持信息共用来源，网址解析与类型检测分别表述。
- 统计匿名安装实例：累计设备、北京时间今日活跃、包含今天的近 30 天活跃。安装 ID 不参与备份，仅上传 ID 和版本。
- 新装及升级先展示说明，默认开启、设置可关闭；只在前台发送，每天成功一次，后续前台使用可重试，关闭即取消，调试及演示不计数。
- 独立 Workers + D1：`usage.hidisiwa.xyz/v1/usage` 接收记录；`stats.hidisiwa.xyz` 和 `/api/summary` 均由 Access 保护，只允许 `822069905@qq.com` 邮箱验证码登录，并在 Worker 内验证 JWT。
- 先配置 Access 再部署服务；禁用 workers.dev 和预览域。完成单元、设备测试，Debug/Release 构建和真实链路检查。

统计只能涵盖新版接入后成功上报的设备，不能补算旧版本使用人数。设备不等于自然人数，重装或清除数据可能重新计数。

实施与验证结果见 `docs/testing/2026-09-11-ui-fixes-private-usage.md`。
