# A 清透实施记录

> 状态（2026-09-10）：A 视觉层已按用户要求回退到原有液体玻璃界面。当前进度与未完成验收以 [2026-09-10-ui-rollback.md](2026-09-10-ui-rollback.md) 为准；本文件保留 A 方案的历史记录，不代表 A 视觉已启用。

按用户批准的 A 方向在现有工作区实施。不提交、不推送，不执行真实选退课或抢课。测试、构建、安装和性能采样按用户要求继续暂停。

## 当前进度

- [x] 核对当前源码和上一轮未完成记录。
- [x] 修复账号间页面内存数据和保存容器的隔离、返回恢复及 Cookie 更新反馈版本归属。
- [x] 公共紧凑顶栏、操作组、透色内容表面及模态操作栏。
- [x] 课程、成绩、课表、抢课、设置、登录与辅助页迁移。
- [x] 图标共享进度、菜单和弹层状态、渲染来源与空闲任务审查（静态完成，设备验证待恢复）。
- [x] A 的浅深色及关键状态稿对应实现。
- [ ] 最新源码构建、会话和设备回归、性能及适配验收（暂停）。

## 已完成但仍需验收的实现

公共 `SystemTopBar` 统一 24/20sp 标题、64dp 最小高度和固定 48dp 操作组；成绩、课程、课表使用统一操作组和状态图标。重复内容卡片使用 `FrostedContent`，课程、成绩、已选、抢课队列、设置辅助列表共享 20dp 模糊和透明度保护。课程详情、筛选改为底部面板；学校配置、添加学校、背景编辑改为可滚动二级表单页。登录页提供回退入口和 `login-screen` 稳定定位标识。

会话版本和请求编号继续覆盖旧回调、旧 Cookie、账号切换、单飞续期和巡检；已选课程旧实现也绑定 `SessionRequestTicket`，并使用结构化表单编码。页面内容按账号保存，成绩和课程列表使用稳定 key，详情请求回调核对版本。

## 必须保留的待验收项

Cookie 恢复后全部提示清理；旧请求、旧 Cookie 写入及旧续期失效；学校/账号切换与 A→B→A；自动/手动单飞及冷却隔离；巡检启停、取消及晚到回调；登录成功/取消返回、页面筛选/子标签/滚动恢复；连续图标和玻璃手势；静止持续出帧；最新 Debug/Release/AndroidTest/lint/uiPreview/benchmark 构建；三轮真实性能对照；浅深色/复杂壁纸/窄屏/横屏/大字号/减少动画/读屏适配。

验证恢复后仅使用 127.0.0.1:5557。API 31/32 与 TalkBack 的设备验证缺口独立记录。上一轮日志只作为检查点证据，不能视作本轮完成验收。
