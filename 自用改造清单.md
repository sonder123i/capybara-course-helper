# 自用改造清单 — zhengfang-apk → 卡皮巴拉教务助手

> 基线：上游 `znjhahaha/zhengfang-apk` @ `main`（v1.0.80, versionCode 80）
> 本地副本：`C:\Users\k2767\projects\zhengfang-apk-triage`
> 改造执行日期：2026-09-17

---

## 一、构建环境（已拉通）

| 项 | 值 | 说明 |
|---|---|---|
| JDK | `D:\Java\jdk-17.0.16+8` | 本机默认 JDK 是 25，**不能**用于构建 |
| Gradle | 8.13 | 已部署到 `~\.gradle\wrapper\dists\gradle-8.13-bin\ap7pdhvhnjtc6mxtzz89gkh0c\`（含 `.ok` 标记） |
| Android SDK | `C:\Users\k2767\AppData\Local\Android\Sdk` | `local.properties` 已写入 |
| 平台别名 | `platforms\android-37` → 联接自 `android-37.0` | AGP 只认 `android-37` |
| build-tools | 35.0.0（构建时自动安装） | |
| adb | `%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe` | 可用，当前无连接设备 |

**构建命令**（PowerShell，工作目录为项目根）：

```powershell
$env:JAVA_HOME = 'D:\Java\jdk-17.0.16+8'
$env:Path = "D:\Java\jdk-17.0.16+8\bin;$env:Path"
.\gradlew.bat assembleDebug --console=plain
```

---

## 二、改造一：切断对作者服务的依赖（5 处）

新增 `app/src/main/java/com/tyust/course/SelfHostConfig.kt` 集中管理开关，各调用点加一处判断短路，
**不改动任何业务逻辑本身**。想恢复上游行为，把对应开关改回 `true`。

| 处 | 文件 | 改法 |
|---|---|---|
| 应用内更新 | `update/UpdateManager.kt:106` | 返回 null → UI 提示「已是最新版本」，不再请求作者仓库，**也杜绝了上游发新版后引导下载作者原包** |
| 远端公告 | `announcement/AnnouncementManager.kt:62` | 返回 `emptyList()` |
| 反馈上报 | `network/FeedbackManager.kt:42 / 126` | 提交返失败、历史返空（原实现会传学号 + 15KB logcat） |
| 匿名统计 | `usage/UsageStatsManager.kt:59` | 关在装配层的 `eligibleBuild`（放逻辑层会让上游 5 个单测变红） |
| 学校适配后端 | `network/SchoolServiceEndpoints.kt` | `BASE_URL` 改指 `school-api.invalid`（保留域名，必定解析失败） |

---

## 三、改造二：品牌层

### 3.1 应用名与包名

| 项 | 原值 | 新值 | 文件 |
|---|---|---|---|
| 应用名 | 教务助手 | **卡皮巴拉教务助手** | `res/values/strings.xml:3` |
| applicationId | `com.tyust.course` | **`com.k2767.course`** | `app/build.gradle:76` |
| 版本号 | 1.0.80 / 80 | **1.0.0 / 1** | `app/version.properties` |
| Gradle 项目名 | CourseSelector | CapybaraCourseHelper | `settings.gradle:27` |

> **`namespace` 保持 `com.tyust.course` 不变**——只改 `applicationId`。全量重命名源码包会牵动 500+ 文件的
> package 声明，回归风险远大于收益。AGP 支持两者不同，APK 的包名以 `applicationId` 为准。

### 3.2 界面文案（共 11 处）

| 文件 | 内容 |
|---|---|
| `ui/screen/LoginScreen.kt:547` | 登录页副标题 |
| `ui/screen/SettingsScreen.kt:309` | 设置页版本行 |
| `ui/screen/UsageNotice.kt:46` | 统计说明文案 |
| `MainActivity.kt:683` | 关于页感谢语 |
| `app/src/benchmark/AndroidManifest.xml:4` | 性能验证变体名 |
| `app/src/uiPreview/AndroidManifest.xml:4` | 预览变体名 |
| `.github/workflows/release.yml:284` | CI 的 Release 文案 |

### 3.3 应用图标

- 用 AI 生成主图（1024×1024，佛系卡皮巴拉泡在薄荷绿水里），**右下角自带「AI生成」水印，已用「取正上方等大背景区域覆盖」的方式修补**
- 缩放出 5 档密度：mdpi 48 / hdpi 72 / xhdpi 96 / xxhdpi 144 / xxxhdpi 192
- 每档同时生成方形（`ic_launcher.png`）与圆形（`ic_launcher_round.png`，4× 超采样后椭圆蒙版，边缘无锯齿）
- 项目无 `mipmap-anydpi-v26` 自适应图标目录，直接替换 PNG 即可
- **附带收益**：原图标每个 1 MB（所有密度共用同一张），替换后 APK 从 35.5 MB 降到 25.8 MB
- 原始素材与处理脚本保留在 `_brand_icon/`（已加入 `.gitignore`，不进仓库）
  - `make_icons.py` 可重复执行，换图标时只需替换脚本里的 `SRC` 路径

### 3.4 许可与文档

- **`LICENSE`**：在 GPL-3.0 全文之前加了版权声明段，声明本程序是「教务助手」的衍生作品、
  原始版权归原作者，并复述 GPL-3.0 的三项核心义务。**GPL 全文本身未改动**。
- **`README.md`**：
  - 标题改为「卡皮巴拉教务助手」，其下加 fork 声明块（指向上游仓库与作者）
  - 徽章区删除指向作者 Release / CI / Stars 的链接，只保留 LICENSE 与上游引用
  - 「二次开发」章节改写为 GPL-3.0 的准确表述，并指出上游那句「不接受私自打包分发」与 GPLv3 第 10 条的冲突
  - 文末新增「与原项目的关系」表格，如实列出改了什么、没改什么

---

## 四、未改动（需要你先决定）

| 项 | 位置 | 原因 |
|---|---|---|
| 学生账号上限 | `manager/StudentLimitManager.kt:14` `MAX_STUDENTS = 3` | 3 个够用；纯本地计数，随时可改 |
| 源码包名 | `namespace 'com.tyust.course'` | 见 3.1 说明，刻意保留 |
| 作者 GitHub 链接 | `SettingsFragment.kt:190`、`SettingsRoute.kt:574`、`MainActivity.kt:716` | 保留算标注来源，符合 GPL 精神；想改随时说 |

---

## 五、验证记录

| 项 | 结果 |
|---|---|
| 编译 | `BUILD SUCCESSFUL in 1m 22s` |
| 单元测试 | **72 suites / 388 tests / 0 failures / 3 skipped**（`cleanTestDebugUnitTest` 全量重跑） |
| APK 包名 | `package: name='com.k2767.course' versionCode='1' versionName='1.0.0'` |
| 应用名 | `application-label:'卡皮巴拉教务助手'` |
| 图标 | 解包确认 10 个 mipmap 文件全部打进 APK，尺寸与生成结果一致 |
| 改动入包 | 反查 dex：`SelfHostConfig` 出现于多个 dex；`school-api.invalid` 已生效 |
| APK 产物 | 25.8 MB（27,082,480 bytes），sha256 `512209D36769031C4D4B8F91E74F76648A205F9DC3F01AA5BBE21C2CE46D428D` |
| 真机验证 | **待做**——`adb devices` 当前无连接设备 |

**改动文件总览**（`git status`）：

```
 M app/build.gradle                                  applicationId
 M app/version.properties                            版本重置
 M app/src/main/res/values/strings.xml               应用名
 M app/src/main/java/.../update/UpdateManager.kt     更新检查
 M app/src/main/java/.../announcement/AnnouncementManager.kt
 M app/src/main/java/.../network/FeedbackManager.kt
 M app/src/main/java/.../network/SchoolServiceEndpoints.kt
 M app/src/main/java/.../usage/UsageStatsManager.kt
 M app/src/main/java/.../ui/screen/LoginScreen.kt
 M app/src/main/java/.../ui/screen/SettingsScreen.kt
 M app/src/main/java/.../ui/screen/UsageNotice.kt
 M app/src/main/java/.../MainActivity.kt
 M app/src/main/AndroidManifest.xml                  未改（label 走 @string/app_name）
 M app/src/benchmark/AndroidManifest.xml             label
 M app/src/uiPreview/AndroidManifest.xml             label
 M gradle/wrapper/gradle-wrapper.properties          镜像
 M settings.gradle                                   rootProject.name
 M .github/workflows/release.yml                     Release 文案
 M README.md                                         fork 声明 + 改动说明
 M LICENSE                                           版权声明段
 M .gitignore                                        忽略 _brand_icon/
新增：app/src/main/java/com/tyust/course/SelfHostConfig.kt
新增：_brand_icon/（已在 .gitignore 中，不进仓库）
替换：app/src/main/res/mipmap-*/ic_launcher{,_round}.png 共 10 个
```

---

## 六、安装注意

1. **新包名 = 全新应用**：`com.k2767.course` 与原版 `com.tyust.course` 互不冲突，可以共存。
   代价是原来的学校配置、账密要重新登录一遍。
2. 版本号已重置为 1.0.0（versionCode 1）。**如果你之前在手机上装过本项目早期版本**（那时还是
   versionCode 80），直接覆盖安装会因版本号降低而失败，需要先卸载。
3. 设备需允许「安装未知来源应用」（`REQUEST_INSTALL_PACKAGES` 已在 manifest）。

---

## 七、后续可做

1. **接自己的更新服务**：建 `version.json` 放静态托管 → 改 `UpdateManager.VERSION_URL` →
   把 `SelfHostConfig.ENABLE_APP_UPDATE_CHECK` 改回 `true`。
   `version.json` 结构：`{ versionCode, versionName, releaseNotes, downloadUrl, forceUpdate }`
2. **固化为独立 commit**：本次改动跨 20 个文件，建议单独提交，方便以后 `git rebase` 上游。
3. **真机验证**：插上设备后可 `adb install -r` 安装验证。
